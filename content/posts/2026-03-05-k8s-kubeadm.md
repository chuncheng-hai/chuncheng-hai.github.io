---

title: 基于kubeadm的3Control Plane 5Worker K8S集群部署

date: 2026-03-05 13:00:00 +0800

slug: k8s-kubeadm-3

description: "基于kubeadm的3Control Plane 5Worker K8S集群部署，实现Control Plane高而可用，Etcd独立部署"

series: ["Linux与运维实践"]


tags: [Linux,Kubernetes,云原生]

disable_first_line_indent: true

toc: true
---


节点命名规则：
环境名称：prod -> 生产环境
集群名称：k8s -> kubernetes集群
节点名称：control-数字 -> Control Plane节点(又称 Master节点)
        worker-数字 -> Worker节点

- VIP 10.142.100.59
- prod-k8s-lb-01 10.142.100.60  Ubuntu 24.04 LTS 2C4G，40G磁盘
- prod-k8s-lb-02 10.142.100.61  Ubuntu 24.04 LTS 2C4G，40G磁盘


- prod-k8s-etcd-01 10.142.100.62 Ubuntu 24.04 LTS 4C8G，40G磁盘
- prod-k8s-etcd-02 10.142.100.63  Ubuntu 24.04 LTS 4C8G，40G磁盘
- prod-k8s-etcd-03 10.142.100.64  Ubuntu 24.04 LTS 4C8G，40G磁盘

- prod-k8s-control-01 10.142.100.65  Ubuntu 24.04 LTS 4C8G，40G磁盘
- prod-k8s-control-02 10.142.100.66  Ubuntu 24.04 LTS 4C8G，40G磁盘
- prod-k8s-control-03 10.142.100.67  Ubuntu 24.04 LTS 4C8G，40G磁盘

- prod-k8s-worker-01  10.142.100.68  Ubuntu 24.04 LTS 2C4G，40G磁盘
- prod-k8s-worker-02  10.142.100.69  Ubuntu 24.04 LTS 2C4G，40G磁盘
- prod-k8s-worker-03  10.142.100.70  Ubuntu 24.04 LTS 2C4G，40G磁盘
- prod-k8s-worker-04  10.142.100.71  Ubuntu 24.04 LTS 2C4G，40G磁盘
- prod-k8s-worker-05  10.142.100.72  Ubuntu 24.04 LTS 2C4G，40G磁盘



各组件版本选择：
- Etcd ：3.5.13
- CRI容器运行时：containerd 2.2.2
- crictl 1.34.0
- kubeadm 1.34.5、kubelet 1.34.5、kubectl 1.34.5
- CNI容器运行接口：
- Kubenetes包管理工具：Helm 3.20.0
- LB HAproxy 2.8.20 Keepalived 2.2.8

## 1. 各节点环境准备

### 1.1 配置 hostname 主机名

{{< cmd role="prod-k8s-lb-01" title="prod-k8s-lb-01 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-lb-01
{{< /cmd >}}

{{< cmd role="prod-k8s-lb-02" title="prod-k8s-lb-02 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-lb-02
{{< /cmd >}}

{{< cmd role="prod-k8s-etcd-01" title="prod-k8s-etcd-01 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-etcd-01
{{< /cmd >}}

{{< cmd role="prod-k8s-etcd-02" title="prod-k8s-etcd-02 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-etcd-02
{{< /cmd >}}

{{< cmd role="prod-k8s-etcd-03" title="prod-k8s-etcd-03 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-etcd-03
{{< /cmd >}}

{{< cmd role="prod-k8s-control-01" title="prod-k8s-control-01 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-control-01
{{< /cmd >}}

{{< cmd role="prod-k8s-control-02" title="prod-k8s-control-02 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-control-02
{{< /cmd >}}

{{< cmd role="prod-k8s-control-03" title="prod-k8s-control-03 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-control-03
{{< /cmd >}}

{{< cmd role="prod-k8s-worker-01" title="prod-k8s-worker-01 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-worker-01
{{< /cmd >}}

{{< cmd role="prod-k8s-worker-02" title="prod-k8s-worker-02 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-worker-02
{{< /cmd >}}

{{< cmd role="prod-k8s-worker-03" title="prod-k8s-worker-03 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-worker-03
{{< /cmd >}}

{{< cmd role="prod-k8s-worker-04" title="prod-k8s-worker-04 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-worker-04
{{< /cmd >}}

{{< cmd role="prod-k8s-worker-05" title="prod-k8s-worker-05 配置主机名" >}}
sudo hostnamectl set-hostname prod-k8s-worker-05
{{< /cmd >}}

### 1.2 配置 /etc/hosts 映射

{{< cmd role="all" title="集群全部节点配置IP与主机名的映射" >}}
# 备份/etc/hosts 文件
sudo cp /etc/hosts \
/etc/hosts.$(date +%F_%H%M%S).before-k8s-cluster-install.bak

cat <<EOF | sudo tee /etc/hosts
10.142.100.62 prod-k8s-etcd-01
10.142.100.63 prod-k8s-etcd-02
10.142.100.64 prod-k8s-etcd-03
10.142.100.65 prod-k8s-control-01
10.142.100.66 prod-k8s-control-02
10.142.100.67 prod-k8s-control-03
10.142.100.68 prod-k8s-worker-01
10.142.100.69 prod-k8s-worker-02
10.142.100.70 prod-k8s-worker-03
10.142.100.71 prod-k8s-worker-04
10.142.100.72 prod-k8s-worker-05
EOF

{{< /cmd >}}

### 1.3 切换国内 apt 源

{{< cmd role="all" title="集群全部节点配置切换国内 apt 源" >}}
# 华为云阿里云二选一即可，本文因为在华为云上创建实验资源，故选择repo.huaweicloud.com
bash <(curl -sSL https://linuxmirrors.cn/main.sh) \
  --source mirrors.aliyun.com \
  --protocol https            \
  --use-intranet-source false \
  --backup true               \
  --upgrade-software false    \
  --clean-cache false         \
  --ignore-backup-tips        \
  --pure-mode

bash <(curl -sSL https://linuxmirrors.cn/main.sh) \
  --source repo.huaweicloud.com \
  --protocol https              \
  --use-intranet-source false   \
  --backup true                 \
  --upgrade-software false      \
  --clean-cache false           \
  --ignore-backup-tips          \
  --pure-mode

sudo apt update && sudo apt upgrade -y
{{< /cmd >}}

### 1.4 关闭 swap + 防火墙

{{< cmd role="all" title="集群全部节点配置关闭 swap + 防火墙" >}}
sudo swapoff -a
sudo sed -i '/swap/s/^/#/' /etc/fstab
sudo ufw disable
{{< /cmd >}}

### 1.5 时间同步

{{< cmd role="all" title="集群全部节点配置时间同步" >}}

{{< /cmd >}}

### 1.6 加载内核模块

{{< cmd role="all" title="集群全部节点配置加载内核模块" >}}
cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF

sudo modprobe overlay
sudo modprobe br_netfilter

# 启用 IPv4 数据包转发
cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
EOF

# 应用 sysctl 参数而不重新启动
sudo sysctl --system
{{< /cmd >}}

## 2. LB负载均衡

Keepalived + HAproxy实现高可用负载均衡

### 2.1 部署配置Keepalived虚IP

{{< cmd role="prod-k8s-lb-01" title="为prod-k8s-lb-01配置Keepalived" >}}
sudo hostnamectl set-hostname prod-k8s-lb-01

apt install -y keepalived

cat > /etc/keepalived/check_haproxy.sh <<'EOF'
#!/bin/bash
# =============================================================================
# Keepalived 健康检查脚本 - 检查 HAProxy 进程和关键端口
# =============================================================================

# 1. 检查 HAProxy 进程是否存在
if ! killall -0 haproxy >/dev/null 2>&1; then
    echo "HAProxy process not found" >&2
    exit 1
fi

# 2. 检查关键端口是否在 LISTEN 状态（支持多个端口）
PORTS=(6443 80)

for PORT in "${PORTS[@]}"; do
    if ! ss -ltn | grep -q ":${PORT} "; then
        echo "HAProxy not listening on port ${PORT}" >&2
        exit 1
    fi
done

# 检查 HAProxy stats 端口是否能正常响应（如果配置了 stats）
if ! curl -sf -m 2 http://127.0.0.1:8404/stats >/dev/null 2>&1; then
    echo "HAProxy stats check failed" >&2
    exit 1
fi

exit 0
EOF

chmod +x /etc/keepalived/check_haproxy.sh

cat > /etc/keepalived/keepalived.conf <<'EOF'
global_defs {
    router_id LVS_MASTER
    script_user root
}

vrrp_script chk_haproxy {
    script "/etc/keepalived/check_haproxy.sh"
    interval 2
    weight -20
    fall 2
    rise 1
}

# 注意修改网卡名interface ens33
vrrp_instance VI_1 {
    state MASTER
    interface ens33
    virtual_router_id 59
    priority 100
    advert_int 1

    nopreempt

    authentication {
        auth_type PASS
        auth_pass 123456
    }

    virtual_ipaddress {
        10.142.100.59
    }

    track_script {
        chk_haproxy
    }
}
EOF


systemctl enable --now keepalived
{{< /cmd >}}

{{< cmd role="prod-k8s-lb-02" title="为prod-k8s-lb-02配置Keepalived" >}}
sudo hostnamectl set-hostname prod-k8s-lb-02

apt install -y keepalived

cat > /etc/keepalived/check_haproxy.sh <<'EOF'
#!/bin/bash
# =============================================================================
# Keepalived 健康检查脚本 - 检查 HAProxy 进程和关键端口
# =============================================================================

# 1. 检查 HAProxy 进程是否存在
if ! killall -0 haproxy >/dev/null 2>&1; then
    echo "HAProxy process not found" >&2
    exit 1
fi

# 2. 检查关键端口是否在 LISTEN 状态（支持多个端口）
PORTS=(6443 80)

for PORT in "${PORTS[@]}"; do
    if ! ss -ltn | grep -q ":${PORT} "; then
        echo "HAProxy not listening on port ${PORT}" >&2
        exit 1
    fi
done

# 检查 HAProxy stats 端口是否能正常响应（如果配置了 stats）
if ! curl -sf -m 2 http://127.0.0.1:8404/stats >/dev/null 2>&1; then
    echo "HAProxy stats check failed" >&2
    exit 1
fi

exit 0
EOF

chmod +x /etc/keepalived/check_haproxy.sh

cat > /etc/keepalived/keepalived.conf <<'EOF'
global_defs {
    router_id LVS_BACKUP
    script_user root
}

vrrp_script chk_haproxy {
    script "/etc/keepalived/check_haproxy.sh"
    interval 2
    weight -20
    fall 2
    rise 1
}

vrrp_instance VI_1 {
    state BACKUP
    interface eth0
    virtual_router_id 59
    priority 90
    advert_int 1

    nopreempt

    authentication {
        auth_type PASS
        auth_pass 123456
    }

    virtual_ipaddress {
        10.142.100.59
    }

    track_script {
        chk_haproxy
    }
}
EOF

systemctl enable --now keepalived
{{< /cmd >}}

### 2.2 部署配置HAproxy

大版本选择2.8 2028-Q2 (LTS)，LTS版本详见[HAProxy](https://www.haproxy.org/)
小版本选择2.8.20 last， last版本详见[HAProxy known bugs for maintenance branch 2.8](https://www.haproxy.org/bugs/bugs-2.8.html)

{{< cmd role="prod-k8s-lb-all" title="为全部LB服务器部署配置HAproxy" >}}
# 安装编译依赖
sudo apt update
sudo apt install -y \
  build-essential   \
  libssl-dev        \
  libpcre2-dev      \
  zlib1g-dev        \
  liblua5.4-dev     \
  libsystemd-dev    \
  libngtcp2-dev \
  libnghttp3-dev

# 下载 HAProxy 2.8.20 的源码包
wget https://www.haproxy.org/download/2.8/src/haproxy-2.8.20.tar.gz

# 解压源码包，并执行解压成功后清理
tar zxf haproxy-2.8.20.tar.gz && rm haproxy-2.8.20.tar.gz 

cd haproxy-2.8.20
make clean

make TARGET=linux-glibc \
     USE_OPENSSL=1      \
     USE_PCRE2=1 \
     USE_ZLIB=1  \
     USE_SYSTEMD=1 \
     USE_LUA=1    \
     USE_PROMEX=1 \
     -j$(nproc)

# 将 `${/app/haproxy}` 和 `${/app/haproxy/bin}` 替换为自定义的实际路径
make PREFIX=/usr/local       \
     SBINDIR=/usr/local/sbin \
     install 

haproxy -v

# 创建目录
mkdir -p /etc/haproxy /var/log/haproxy /run/haproxy

cat <<EOF | sudo tee /etc/haproxy/haproxy.cfg
# ========================
# 全局配置
# ========================
global
    log /dev/log local0        # 使用 syslog 记录日志（local0 facility）
    maxconn 100000            # HAProxy 最大并发连接数
    daemon                    # 以守护进程方式运行

# ========================
# 默认参数（frontend/backend继承）
# ========================
defaults
    log global                # 继承 global 中的日志配置
    mode tcp                  # 使用四层 TCP 模式
    option tcplog             # 使用 TCP 日志格式

    timeout connect 5s        # 后端连接超时
    timeout client  1m        # 客户端空闲超时
    timeout server  1m        # 后端响应超时

# ========================
# 前端（K8s API入口）
# ========================
frontend k8s_api
    bind *:6443                   # 监听K8s API Server 6443端口
    default_backend k8s_api_back  # 默认转发到后端集群

# ========================
# 后端（K8s control plane）
# ========================
backend k8s_api_back
    balance leastconn         # 最少连接负载均衡
    option tcp-check          # 启用 TCP 健康检查

    # fall 3：连续3次失败才判定下线
    # rise 2：连续2次成功恢复上线
    # maxconn：限制单节点最大连接数（防止压垮）

    server prod-k8s-control-01 10.142.100.65:6443 check fall 3 rise 2 maxconn 2000
    server prod-k8s-control-02 10.142.100.66:6443 check fall 3 rise 2 maxconn 2000
    server prod-k8s-control-03 10.142.100.67:6443 check fall 3 rise 2 maxconn 2000
EOF

# 验证配置
haproxy -c -f /etc/haproxy/haproxy.cfg

# 启动HAProxy
haproxy -f /etc/haproxy/haproxy.cfg
{{< /cmd >}}

## 3. 部署配置Etcd独立集群

{{< cmd role="prod-k8s-etcd-all" title="prod-k8s-etcd-all 安装etcd" >}}
ETCD_VERSION=3.5.13

wget https://github.com/etcd-io/etcd/releases/download/v${ETCD_VERSION}/etcd-v${ETCD_VERSION}-linux-amd64.tar.gz
tar -xvf etcd-v${ETCD_VERSION}-linux-amd64.tar.gz
cd etcd-v${ETCD_VERSION}-linux-amd64

cp etcd etcdctl /usr/local/bin/

# 下载证书生成工具cfssl，更多版本详见https://github.com/cloudflare/cfssl/releases/tag/v1.6.4/
# 下载异常可基于https://gh-proxy.com/转换github代理链接
wget https://github.com/cloudflare/cfssl/releases/download/v1.6.4/cfssl_1.6.4_linux_amd64 -O /usr/local/bin/cfssl
wget https://github.com/cloudflare/cfssl/releases/download/v1.6.4/cfssljson_1.6.4_linux_amd64 -O /usr/local/bin/cfssljson
wget https://github.com/cloudflare/cfssl/releases/download/v1.6.4/cfssl-certinfo_1.6.4_linux_amd64 -O /usr/local/bin/cfssl-certinfo

chmod +x /usr/local/bin/cfssl*
# 验证
cfssl version  
{{< /cmd >}}



{{< cmd role="prod-k8s-etcd-01" title="prod-k8s-etcd-01 生成自签名 TLS 证书并分发" >}}
#  etcd 必须启用 TLS
mkdir -p ~/etcd-tls && cd ~/etcd-tls
mkdir -p certs config

# 创建 CA 配置
cat > config/ca-csr.json << EOF
{
  "CN": "etcd-ca",
  "key": {
    "algo": "rsa",
    "size": 4096
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "etcd",
      "OU": "CA"
    }
  ]
}
EOF

cat > config/ca-config.json << EOF
{
  "signing": {
    "default": {
      "usages": ["signing", "key encipherment", "server auth", "client auth"],
      "expiry": "876000h"
    }
  }
}
EOF


cfssl gencert -initca config/ca-csr.json | cfssljson -bare certs/ca

cat > config/req-csr.json << EOF
{
  "CN": "etcd",
  "hosts": [
    "prod-k8s-etcd-01",
    "prod-k8s-etcd-02",
    "prod-k8s-etcd-03",
    "10.142.100.62",
    "10.142.100.63",
    "10.142.100.64"
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "etcd",
      "OU": "etcd-cluster"
    }
  ]
}
EOF

# server 证书（用于 --cert-file / --key-file）
cfssl gencert \
  -ca=certs/ca.pem \
  -ca-key=certs/ca-key.pem \
  -config=config/ca-config.json \
  config/req-csr.json | cfssljson -bare certs/etcd-server

# peer 证书（用于 --peer-cert-file / --peer-key-file）
cfssl gencert \
  -ca=certs/ca.pem \
  -ca-key=certs/ca-key.pem \
  -config=config/ca-config.json \
  config/req-csr.json | cfssljson -bare certs/etcd-peer


# 生成 apiserver-etcd-client 证书
cat > config/apiserver-etcd-client-csr.json << EOF
{
  "CN": "kube-apiserver-etcd-client",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "system:masters",
      "OU": "Kubernetes"
    }
  ]
}
EOF

cfssl gencert \
  -ca=certs/ca.pem \
  -ca-key=certs/ca-key.pem \
  -config=config/ca-config.json \
  config/apiserver-etcd-client-csr.json | cfssljson -bare certs/apiserver-etcd-client

# 创建目标目录
for hostname in prod-k8s-etcd-01 prod-k8s-etcd-02 prod-k8s-etcd-03; do
  ssh root@${hostname} "mkdir -p /etc/etcd/ssl"
  scp -r certs/* root@${hostname}:/etc/etcd/ssl/
done
{{< /cmd >}}

{{< cmd role="prod-k8s-etcd-all" title="prod-k8s-etcd-all 配置etcd" >}}
chown -R root:root /etc/etcd/ssl
chmod 600 /etc/etcd/ssl/*-key.pem
ls -la /etc/etcd/ssl/

ETCD01="10.142.100.62"
ETCD02="10.142.100.63"
ETCD03="10.142.100.64"
ETCD_NAME="etcd01"
NODE_IP="${ETCD01}"

cat > /etc/systemd/system/etcd.service << EOF
# /etc/systemd/system/etcd.service
[Unit]
Description=etcd key-value store
After=network.target
Wants=network-online.target

[Service]
Type=notify

ExecStart=/usr/local/bin/etcd \\
  --name=${ETCD_NAME} \\
  --data-dir=/var/lib/etcd \\
  --listen-client-urls=https://${NODE_IP}:2379 \\
  --advertise-client-urls=https://${NODE_IP}:2379 \\
  --listen-peer-urls=https://${NODE_IP}:2380 \\
  --initial-advertise-peer-urls=https://${NODE_IP}:2380 \\
  --initial-cluster=etcd01=https://${ETCD01}:2380,etcd02=https://${ETCD02}:2380,etcd03=https://${ETCD03}:2380 \\
  --initial-cluster-token=etcd-cluster \\
  --initial-cluster-state=new \\
  --cert-file=/etc/etcd/ssl/etcd-server.pem \\
  --key-file=/etc/etcd/ssl/etcd-server-key.pem \\
  --trusted-ca-file=/etc/etcd/ssl/ca.pem \\
  --client-cert-auth=true \\
  --peer-cert-file=/etc/etcd/ssl/etcd-peer.pem \\
  --peer-key-file=/etc/etcd/ssl/etcd-peer-key.pem \\
  --peer-trusted-ca-file=/etc/etcd/ssl/ca.pem \\
  --peer-client-cert-auth=true \\
  --enable-v2=false

Restart=on-failure
RestartSec=5
LimitNOFILE=65536

# 避免 etcd 未 ready 导致 systemd 超时
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable --now etcd
systemctl status etcd

ETCDCTL_API=3 etcdctl \
  --endpoints=https://${ETCD01}:2379,https://${ETCD02}:2379,https://${ETCD03}:2379 \
  --cacert=/etc/etcd/ssl/ca.pem \
  --cert=/etc/etcd/ssl/etcd-server.pem \
  --key=/etc/etcd/ssl/etcd-server-key.pem \
  endpoint health --cluster


# 因为所有etcd节点互相验证 TLS，CA 是信任根，所以ca.crt是唯一需要跨节点分发的文件

{{< /cmd >}}

## 4. K8S各节点安装并配置 CRI 容器运行时插件

Kubernetes集群要求使用符合容器运行时接口（CRI）的容器运行时，常见的有如下三个

- containerd
- CRI-O
- cri-dockerd(Docker 方案)
更多详见[容器运行时](https://kubernetes.io/zh-cn/docs/setup/production-environment/container-runtimes/#cri-versions)

### 4.1 安装 containerd

{{< cmd role="all" title="集群全部节点安装 containerd" >}}
sudo apt install -y ca-certificates curl gnupg
sudo mkdir -p /etc/apt/keyrings

curl -fsSL https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://mirrors.aliyun.com/docker-ce/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update

sudo apt install -y containerd.io

# 查看containerd版本，本文使用containerd.io v2.2.2 版本
containerd --version
{{< /cmd >}}


### 4.2 配置 containerd

{{< cmd role="all" title="集群全部节点配置 containerd" >}}
# 生成默认配置
sudo mkdir -p /etc/containerd
containerd config default | sudo tee /etc/containerd/config.toml

# 配置pause 容器镜像源为阿里云镜像源
sudo sed -i 's#registry.k8s.io/pause:3.10#registry.aliyuncs.com/google_containers/pause:3.10#g' /etc/containerd/config.toml

# 配置 systemd cgroup 驱动
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/' /etc/containerd/config.toml

# 在[plugins."io.containerd.grpc.v1.cri".registry]行下
# 配置文件第54行的 config_path = "" 改为 config_path = "/etc/containerd/certs.d"
vim /etc/containerd/config.toml 

# 配置国内docker镜像加速
sudo mkdir -p /etc/containerd/certs.d/docker.io

cat <<EOF | sudo tee /etc/containerd/certs.d/docker.io/hosts.toml
[host."https://docker.gh-proxy.org"]
  capabilities = ["pull", "resolve"]

[host."https://docker.1ms.run"]
  capabilities = ["pull", "resolve"]

[host."https://docker.xuanyuan.me"]
  capabilities = ["pull", "resolve"]

# 阿里云镜像加速器
[host."https://registry.cn-hangzhou.aliyuncs.com"]
  capabilities = ["pull", "resolve"]

# 网易云镜像加速器
[host."https://hub-mirror.c.163.com"]
  capabilities = ["pull", "resolve"]

# 腾讯云镜像加速器
[host."https://mirror.ccs.tencentyun.com"]
  capabilities = ["pull", "resolve"]

# 中国科技大学镜像
[host."https://docker.mirrors.ustc.edu.cn"]
  capabilities = ["pull", "resolve"]
EOF

# 重启 containerd
sudo systemctl daemon-reload
sudo systemctl enable --now containerd
sudo systemctl restart containerd
{{< /cmd >}}

### 4.3 安装配置 crictl

{{< cmd role="all" title="集群全部节点安装配置 crictl" >}}
# K8s集群与crictl的大版本号需要保持一致
VERSION="v1.34.0"

wget https://github.com/kubernetes-sigs/cri-tools/releases/download/$VERSION/crictl-$VERSION-linux-amd64.tar.gz
# 上面的链接下载异常，可尝试如下链接，走大陆github代理下载
wget https://gh-proxy.org/https://github.com/kubernetes-sigs/cri-tools/releases/download/$VERSION/crictl-$VERSION-linux-amd64.tar.gz

sudo tar zxvf crictl-$VERSION-linux-amd64.tar.gz -C /usr/local/bin
rm -f crictl-$VERSION-linux-amd64.tar.gz

# 配置 crictl 指向 containerd
cat <<EOF | sudo tee /etc/crictl.yaml
runtime-endpoint: unix:///run/containerd/containerd.sock
image-endpoint: unix:///run/containerd/containerd.sock
timeout: 10
debug: false
pull-image-on-create: false
EOF

# 检查是否安装成功并连上 containerd
crictl version

# 拉取busybox容器镜像测试
sudo crictl pull busybox:latest
{{< /cmd >}}

## 5. 安装kubeadm kubelet  kubectl

{{< cmd role="all" title="集群全部节点安装kubeadm kubelet  kubectl" >}}
# 导入 GPG key
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.34/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg

# 使用国内THU的Kubernetes镜像源
cat <<EOF | sudo tee /etc/apt/sources.list.d/kubernetes.list
deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://mirrors.tuna.tsinghua.edu.cn/kubernetes/core:/stable:/v1.34/deb/ /
EOF

sudo apt update

# 安装指定版本
sudo apt install -y kubelet=1.34.5-1.1 kubeadm=1.34.5-1.1 kubectl=1.34.5-1.1

# 锁定版本，防止自动升级
sudo apt-mark hold kubelet kubeadm kubectl

# 配置kubelet开机自启并启动
sudo systemctl enable --now kubelet
{{< /cmd >}}

## 6. Control 节点初始化集群
kube-apiserver:v1.34.5
kube-controller-manager:v1.34.5
kube-scheduler:v1.34.5
kube-proxy:v1.34.5
coredns:v1.12.1
pause:3.10.1
etcd:3.6.5-0

{{< cmd role="prod-k8s-control-all" title="prod-k8s-control-all 全部Control Plane节点拉取Etcd CA证书" >}}
# 全部Control Plane 节点 拉取Etcd CA证书
scp root@prod-k8s-etcd-01:/root/etcd-tls/ssl/ca.pem                        /etc/kubernetes/pki/etcd/ca.crt
scp root@prod-k8s-etcd-01:/root/etcd-tls/ssl/apiserver-etcd-client.pem     /etc/kubernetes/pki/apiserver-etcd-client.crt
scp root@prod-k8s-etcd-01:/root/etcd-tls/ssl/apiserver-etcd-client-key.pem /etc/kubernetes/pki/apiserver-etcd-client.key

# 设置权限
chmod 600 /etc/kubernetes/pki/etcd/ca.crt \
          /etc/kubernetes/pki/apiserver-etcd-client.crt \
          /etc/kubernetes/pki/apiserver-etcd-client.key
chown root:root /etc/kubernetes/pki/etcd -R
{{< /cmd >}}

{{< cmd role="prod-k8s-control-01" title="prod-k8s-control-01 节点初始化集群" >}}
# 拉取阿里云镜像
sudo kubeadm config images pull \
  --image-repository registry.aliyuncs.com/google_containers \
  --kubernetes-version=v1.34.5


# 初始化（Flannel 专用 CIDR）
kubeadm config print init-defaults > kubeadm-config.yaml

# control-plane-endpoint与apiserver-cert-extra-sans配置HAproxy的虚拟IP 10.142.100.59
cat > kubeadm-config.yaml << 'EOF'
apiVersion: kubeadm.k8s.io/v1beta4
kind: ClusterConfiguration
kubernetesVersion: v1.34.5
imageRepository: registry.aliyuncs.com/google_containers

controlPlaneEndpoint: "10.142.100.59:6443"

networking:
  podSubnet: "10.244.0.0/16"
  serviceSubnet: "10.96.0.0/12"

apiServer:
  certSANs:
    - 10.142.100.59
    - 10.142.100.62
    - 10.142.100.63
    - 10.142.100.64
    - 127.0.0.1
    - localhost
    - kubernetes
    - kubernetes.default
    - kubernetes.default.svc
    - kubernetes.default.svc.cluster.local

etcd:
  external:
    endpoints:
      - https://10.142.100.62:2379
      - https://10.142.100.63:2379
      - https://10.142.100.64:2379
    caFile: /etc/kubernetes/pki/etcd/ca.crt
    certFile: /etc/kubernetes/pki/apiserver-etcd-client.crt
    keyFile: /etc/kubernetes/pki/apiserver-etcd-client.key

---
apiVersion: kubeadm.k8s.io/v1beta4
kind: InitConfiguration
localAPIEndpoint:
  advertiseAddress: 10.142.100.62
  bindPort: 6443
nodeRegistration:
  criSocket: unix:///run/containerd/containerd.sock

---
apiVersion: kubeproxy.config.k8s.io/v1alpha1
kind: KubeProxyConfiguration
mode: ipvs

---
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
cgroupDriver: systemd
EOF

sudo kubeadm init              \
  --config kubeadm-config.yaml \
  --upload-certs

# 配置 kubectl
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
{{< /cmd >}}

## 7. 安装并配置 CNI 网络插件

Kubernetes 默认没有 Pod 网络，为了解决多节点组网核心问题，Kubernetes使用一个叫容器网络接口(CNI，Container Network Interface)的标准，因此在kubeadm init初始化之后必须安装一个 CNI 网络插件，常见的CNI插件如下：
- Flannel(推荐新手) 简单稳定，小集群常用，默认不支持 网络隔离(NetworkPolicy)
- Calico(生产常用) 支持 NetworkPolicy
- Cilium eBPF 高性能
- Canal

{{< cmd role="prod-k8s-control-01" title="prod-k8s-control-01 安装 CNI" >}}
# 将flannel应用到集群，若网络异常，可使用github代理下载链接
kubectl apply -f  https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml

# 下方为github代理下载链接，浏览器访问https://gh-proxy.com/可自行探索
kubectl apply -f  https://gh-proxy.org/https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml


# 查看flannel相关的三个容器状态是否均为Running
kubectl get pod -A
{{< /cmd >}}

## 8. 配置节点加入集群

## 8.1 Control节点加入集群

## 8.2 Worker节点加入集群
{{< cmd role="prod-k8s-worker-01 " title="配置prod-k8s-worker-01 节点加入集群" >}}
kubeadm join 192.168.101.40:6443 --token gqdraq.rpj19i8km5p1pplq \
	--discovery-token-ca-cert-hash sha256:e79e9142e0a25aa41dcea23cc1e537c933474683539eb05bd09a3c4d44e9b350 
{{< /cmd >}}

{{< cmd role="prod-k8s-worker-02 " title="配置prod-k8s-worker-02 节点加入集群" >}}
kubeadm join 192.168.101.40:6443 --token gqdraq.rpj19i8km5p1pplq \
	--discovery-token-ca-cert-hash sha256:e79e9142e0a25aa41dcea23cc1e537c933474683539eb05bd09a3c4d44e9b350 
{{< /cmd >}}


{{< cmd role="prod-k8s-control-01" title="prod-k8s-control-01 控制节点查看集群状态" >}}
# 查看集群各节点状态是否均为Ready
kubectl get nodes

# 测试 Pod 跨节点通信
kubectl run nginx --image=nginx --port=80
kubectl expose pod nginx --port=80 --type=NodePort
kubectl get svc
{{< /cmd >}}

---

## metrics-server


## 7. 安装Kubernetes 包管理 Helm

{{< cmd role="prod-k8s-control-01" title="prod-k8s-control-01 安装Helm" >}}
# 安装脚本，若下载异常，替换为华为云镜像安装
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

# 华为云镜像安装helm
wget https://mirrors.huaweicloud.com/helm/v3.20.0/helm-v3.20.0-linux-amd64.tar.gz
tar -zxvf helm-v3.20.0-linux-amd64.tar.gz
sudo mv linux-amd64/helm /usr/local/bin/
chmod +x /usr/local/bin/helm

helm version

helm repo add stable https://kubernetes.oss-cn-hangzhou.aliyuncs.com/charts
helm repo add kaiyuanshe http://mirror.kaiyuanshe.cn/kubernetes/charts
helm repo add azure http://mirror.azure.cn/kubernetes/charts

helm repo list
{{< /cmd >}}

## Ingress Controller

## MetalLB（裸机 LB）

## 8. StorageClass

Longhorn

## CertManager

## Kubernetes Dashboard

## Rancher

必须在 Ingress + Storage 之后安装

## 监控系统

可观测性
Prometheus + Grafana
监控：

Node

Pod

etcd

API Server

## 日志系统

EFK
ELK
Loki + Grafana(资源占用小)

## CI/CD + GitOps

Gitlab
   ↓
Jenkins
   ↓
Harbor
   ↓
ArgoCD
   ↓
Kubernetes

## RBAC / ServiceAccount

1. 全部节点基础环境配置
2. 全部节点CRI运行时 containerd/CRI-O/cri-dockerd/
3. 控制节点 kubeadm init
4. 控制节点 CNI 网络 Flannel/Calico/Cilium/Canal
5. 加入worker节点
6. 控制节点 Ingress Controller(外部访问) ingress-nginx
7. 控制节点 安装Helm 
8. 控制节点 StorageClass(持久化存储) Longhorn/Ceph
9. 
10. 控制节点 Rancher
11. 控制节点 Monitoring / Logging
12. 控制节点 配置 Gitlab Jenkins Harbor ArgoCD (GitOps)
13. 控制节点 创建监控系统（Prometheus + Grafana）
14. 控制节点 配置日志系统（EFK/ELK/Loki）
MetalLB（裸机负载均衡）
CertManager（HTTPS 自动证书）
metrics-server
ingress-nginx
kubernetes-dashboard
配置 RBAC / ServiceAccount（生产安全）