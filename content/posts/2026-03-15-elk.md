---

title: 基于ELK的日志收集系统部署

date: 2026-03-15 13:00:00 +0800

slug: elk

description: "基于ELK的日志收集系统部署，Elasticsearch -> Logstash -> Kibana -> Filebeat"

series: ["Linux与运维实践"]

tags: [Linux]

disable_first_line_indent: true

toc: true
---

>部署注意： 
>- 整个 Elastic Stack 必须使用同一个版本号(本文选择8.19.12稳定版本)。因为Elasticsearch、Logstash、Kibana与Filebeat都是 Elastic Stack 产品，所以在部署过程中，各组件必须基于相同的版本号安装。
> - 部署顺序：Elasticsearch -> Logstash -> Kibana -> Filebeat
>- 部署全过程基于elastic用户进行操作

## elastic用户

## 部署Elasticsearch

本节内容基于[Elasticsearch官方部署文档](https://www.elastic.co/guide/en/elasticsearch/reference/8.12/install-elasticsearch.html)

Ubuntu安装命令

```bash
wget https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-8.19.12-amd64.deb

sudo dpkg -i elasticsearch-8.19.12-amd64.deb
```

RedHat安装命令

```bash
wget https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-8.19.12-x86_64.rpm

sudo rpm --install elasticsearch-8.19.12-x86_64.rpm
```

编辑/etc/elasticsearch/elasticsearch.yml配置文件
network.host: 0.0.0.0
discovery.type: single-node

sudo /bin/systemctl daemon-reload
sudo /bin/systemctl --now elasticsearch.service

```bash


sudo tee /etc/sysctl.d/elasticsearch.conf <<EOF
vm.max_map_count=262144
fs.file-max=65536
EOF

sudo sysctl -p /etc/sysctl.d/elasticsearch.conf

# 建议将 elastic 密码存储为 shell 中的环境变量
export ELASTIC_PASSWORD="your_password"
```

## 部署Logstash

[Logstash官方部署文档](https://www.elastic.co/docs/reference/logstash/installing-logstash)
[Logstash各版本下载地址](https://www.elastic.co/downloads/logstash)

```bash
wget https://artifacts.elastic.co/downloads/logstash/logstash-8.19.12-amd64.deb

sudo dpkg -i logstash-8.19.12-amd64.deb
```

```bash
wget https://artifacts.elastic.co/downloads/logstash/logstash-8.19.12-x86_64.rpm

sudo rpm --install logstash-8.19.12-x86_64.rpm
```

编辑/etc/logstash/conf.d/logstash.conf配置文件
pipeline

```
input {
  beats {
    port => 5044
  }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
  }
}
```

sudo /bin/systemctl daemon-reload
sudo /bin/systemctl --now logstash.service

## 部署Kibana

[Kibana官方部署文档](https://www.elastic.co/docs/deploy-manage/deploy/self-managed/install-kibana)

```bash
wget https://artifacts.elastic.co/downloads/kibana/kibana-8.19.12-amd64.deb

sudo dpkg -i kibana-8.19.12-amd64.deb
```

/etc/kibana/kibana.yml

server.host: "0.0.0.0"
elasticsearch.hosts: ["http://localhost:9200"]

sudo /bin/systemctl daemon-reload
sudo /bin/systemctl enable --now kibana.service

## 部署Filebeat

[Filebeat官方部署文档](https://www.elastic.co/docs/reference/beats/filebeat/filebeat-installation-configuration)

curl -L -O https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-8.19.12-amd64.deb
sudo dpkg -i filebeat-8.19.12-amd64.deb

curl -L -O https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-8.19.12-x86_64.rpm
sudo rpm -vi filebeat-8.19.12-x86_64.rpm

```bash
curl -L -O https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-8.19.12-linux-x86_64.tar.gz

tar xzvf filebeat-8.19.12-linux-x86_64.tar.gz

./filebeat modules enable nginx
```

/etc/filebeat/filebeat.yml

filebeat.inputs:
- type: log
  paths:
    - /var/log/*.log

output.logstash:
  hosts: ["localhost:5044"]

sudo /bin/systemctl daemon-reload
sudo /bin/systemctl enable --now filebeat.service


日志收集数据流：应用服务器(日志产生者) → Filebeat(采集代理) → Logstash(日志处理引擎 ) → Elasticsearch(日志存储与搜索引擎) → Kibana(日志可视化平台)