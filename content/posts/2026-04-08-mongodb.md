---

title: MongoDB简明教程
date: 2026-04-08 14:00:00 +0800

slug: mongodb

description: "Mongodb"

series: ["Linux与运维实践"]

tags: [Linux]

disable_first_line_indent: true

toc: true
---

```bash
wget -qO - https://www.mongodb.org/static/pgp/server-8.0.asc | gpg -o /etc/apt/keyrings/mongodb-server-8.0.gpg --dearmor

echo "deb [signed-by=/etc/apt/keyrings/mongodb-server-8.0.gpg] https://mirrors.tuna.tsinghua.edu.cn/mongodb/apt/ubuntu noble/mongodb-org/8.0 multiverse" > /etc/apt/sources.list.d/mongodb.list

apt-get update
apt-get install -y mongodb-org

sudo systemctl enable --now mongod
sudo systemctl status mongod

# 进入 MongoDB Shell
mongosh

db.version()   // 查看版本
use test       // 切换到 test 数据库
db.test.insertOne({name: "学习MongoDB", time: new Date()})  // 插入一条文档
db.test.find() // 查询

use admin
db.createUser({
  user: "admin",
  pwd: "你的强密码",
  roles: [{ role: "root", db: "admin" }]
})

/etc/mongod.conf

```


```bash
docker run -d swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/library/mongo:7.0.12
```