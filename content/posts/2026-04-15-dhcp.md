---

title: DHCP服务部署

date: 2026-04-15 22:00:00 +0800

slug: dhcp

description: "DHCP服务部署"

series: ["Linux与运维实践"]

tags: [Linux]

disable_first_line_indent: true

toc: true
---

高可用部署
ISC DHCP Failover
Keepalived

primary
secondary

```bash
apt update
apt install isc-dhcp-server -y

/etc/default/isc-dhcp-server
INTERFACESv4="eth0"

/etc/dhcp/dhcpd.conf
authoritative;

default-lease-time 600;
max-lease-time 7200;

subnet 192.168.10.0 netmask 255.255.255.0 {
    range 192.168.10.100 192.168.10.200;
    option routers 192.168.10.1;
    option subnet-mask 255.255.255.0;
    option domain-name-servers 192.168.10.2, 8.8.8.8;
    option domain-name "corp.local";
    pool {
        failover peer "dhcp-failover";
        range 192.168.10.100 192.168.10.200;
    }
}

# 静态分配（固定IP）
host k8s-node-1 {
    hardware ethernet 00:11:22:33:44:55;
    fixed-address 192.168.10.10;
}
failover peer "dhcp-failover" {
  primary;
  address 192.168.10.11;
  port 647;
  peer address 192.168.10.12;
  peer port 647;
  max-response-delay 60;
  max-unacked-updates 10;
  load balance max seconds 3;
}


systemctl enable --now isc-dhcp-server
systemctl status isc-dhcp-server
```



## Kea DHCP方案

```bash
apt install -y kea-dhcp4-server

# IPAM组件

# DHCP + DNS 联动
ddns-update-style interim;
update-static-leases on;

# 防止非法DHCP
apt install dhcpd-pool-check

# 防止DHCP spoofing（交换机）
开启：
- DHCP Snooping（企业交换机）
```

中小企业	ISC DHCP + Keepalived
大型企业	Kea DHCP + NetBox + HA
云原生	CoreDNS + Kea API